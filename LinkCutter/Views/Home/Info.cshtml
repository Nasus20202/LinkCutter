@using LinkCutter
@using LinkCutter.Models
@model LinkInfoModel
@{
    Layout = "_Layout";
}

<div class="bg-blue vertical-center" style="padding:20px; border: 3px solid #a8dadc; border-radius: 10px;">
    <table class="table text-white">
        <tbody>
            <tr>
                <td>Code</td>
                <td>@Model.Link.Code</td>
            </tr>
            <tr>
                <td>Link</td>
                <td>@Model.Link.Code</td>
            </tr>
            <tr>
                <td>Times used</td>
                <td>@Model.Link.TimesUsed.ToString()</td>
            </tr>
            <tr>
                <td>Date added</td>
                <td>@Model.Link.TimeAdded.ToString()</td>
            </tr>
        </tbody>
    </table>
    </div>
